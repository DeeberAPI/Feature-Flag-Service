# Feature-Flag-Service
# Feature Flag Service  A simple feature flag API inspired by PostHog.  ### Endpoints GET /flags GET /flags/:key POST /flags/:key { "enabled": true/false } DELETE /flags/:key  ### Run npm install express node server.js
